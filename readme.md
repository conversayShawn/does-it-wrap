# Testing if [Cucumber](https://github.com/badeball/cypress-cucumber-preprocessor) and [Steps](https://github.com/filiphric/cypress-plugin-steps) plugins automatically wrap `before` or `beforeEach` hooks

## Install & Run

```
npm i
npx cypress open
```